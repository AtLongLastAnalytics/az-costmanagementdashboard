# Screenshots

The top-level `README.md` embeds three images from this folder. Save them here with
these exact names (PNG), captured with the demo seed data loaded:

- `dashboard.png` — the Azure Workbook (trend + budget line, category bars, budget-status table).
- `alert-email.png` — a budget-alert email (showing the "% of budget / threshold crossed / forecast" line).
- `weekly-report.png` — the weekly report email (budget line, by-category, coloured week-over-week movers).

Tip: crop to the content and keep each under ~500 KB so the README loads quickly.
